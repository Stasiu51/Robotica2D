using System.Collections;
using System.Collections.Generic;
using Serialisation;
using UnityEditor;
using UnityEngine;

public class testing : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        testsave.tryserialise();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
